from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Machine learning model to build & train voice based experiments',
    author='sgmoorthy',
    license='MIT',
)
